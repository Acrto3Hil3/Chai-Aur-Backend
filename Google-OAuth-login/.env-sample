# Google OAuth Credentials

# Replace the values below with your own credentials from the Google API Console:

# 1. Go to https://console.developers.google.com/

# 2. Create a new project or select an existing one.

# 3. Enable "Google+ API" or "OAuth 2.0 Client IDs" under APIs & Services.

# 4. Create OAuth 2.0 credentials and copy the Client ID and Client Secret here.

# 5. Set the redirect URI below to match the one you configure in the Google API Console.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# Example: http://localhost:3001/api/auth/google/callback

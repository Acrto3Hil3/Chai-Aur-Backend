SERVER_PORT=your_server_port_number

# DATABASE CONFIGURATION
DATABASE_NAME=your_database_name_here         # Enter your database name
DATABASE_USER_NAME=your_database_user_here    # Enter your database username
DATABASE_USER_PASSWORD=your_password_here     # Enter your database password
DATABASE_SCHEMA_NAME=your_schema_name_here    # Enter your database schema name
DATABASE_HOST_NAME=your_host_here             # Enter your database host (e.g., localhost)
DATABASE_PORT_NO=5432                         # Enter your database port (default for PostgreSQL is 5432)

# JWT secret key for signing tokens
JWT_SECRET_KEY=your_jwt_secret_key_here       # Generate a strong secret key (e.g., using an online generator or `openssl rand -hex 32`)

# Instructions:
# - You can get your database credentials from your database provider or your local database setup.
# - For JWT_SECRET_KEY, generate a secure random string and keep it secret.